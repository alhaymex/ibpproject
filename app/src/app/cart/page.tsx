import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: "My Cart",
};
const page: React.FC = () => {
  return <div>Your Cart is Here!</div>;
};

export default page;
